#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0);
	
	string s, t;
	cin >> s >> t;
	if (s.size()<t.size()) cout << "no";
	else cout << "go";

	return 0;
}
